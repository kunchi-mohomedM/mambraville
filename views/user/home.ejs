<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>E-commerce Store</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.5.2/flowbite.js"
        integrity="sha512-LThUFuq6Y8DTeSxG7VgJQu+3slKAhZ1u3z1EJdZESaVuQIEJ7TXC/DV8xNNEDFW01iWF4tUuzeC4RdiyrRPdRw=="
        crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
</head>

<body class="bg-gray-100">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
        integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <header class="bg-white shadow-sm">
        <%-include("../../views/partials/user/header")-%>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
        <!-- Hero Carousel -->


        <div id="default-carousel" class="relative w-full" data-carousel="slide">
            <!-- Carousel wrapper -->
            <div class="relative h-56 overflow-hidden rounded-lg md:h-96">
                <!-- Item 1 -->
                <div class="hidden duration-700 ease-in-out" data-carousel-item>
                    <img src="\images\pngtree-innovative-3d-mobile-mockup-paired-with-web-development-banner-and-laptop-picture-image_5592791.jpg"
                        class="absolute block w-full -translate-x-1/2 -translate-y-1/2 top-1/2 left-1/2" alt="...">
                </div>
                <!-- Item 2 -->
                <div class="hidden duration-700 ease-in-out" data-carousel-item>
                    <img src="\images\f3832e180145769.6505ae76214ca.jpg"
                        class="absolute block w-full -translate-x-1/2 -translate-y-1/2 top-1/2 left-1/2" alt="...">
                </div>
                <!-- Item 3 -->
                <div class="hidden duration-700 ease-in-out" data-carousel-item>
                    <img src="\images\banner_new1-min.jpg"
                        class="absolute block w-full -translate-x-1/2 -translate-y-1/2 top-1/2 left-1/2" alt="...">
                </div>
            </div>
            <!-- Slider indicators -->
            <div class="absolute z-30 flex -translate-x-1/2 bottom-5 left-1/2 space-x-3 rtl:space-x-reverse">
                <button type="button" class="w-3 h-3 rounded-full" aria-current="true" aria-label="Slide 1"
                    data-carousel-slide-to="0"></button>
                <button type="button" class="w-3 h-3 rounded-full" aria-current="false" aria-label="Slide 2"
                    data-carousel-slide-to="1"></button>
                <button type="button" class="w-3 h-3 rounded-full" aria-current="false" aria-label="Slide 3"
                    data-carousel-slide-to="2"></button>
            </div>
            <!-- Slider controls -->
            <button type="button"
                class="absolute top-0 start-0 z-30 flex items-center justify-center h-full px-4 cursor-pointer group focus:outline-none"
                data-carousel-prev>
                <span
                    class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-white/30 dark:bg-gray-800/30 group-hover:bg-white/50 dark:group-hover:bg-gray-800/60 group-focus:ring-4 group-focus:ring-white dark:group-focus:ring-gray-800/70 group-focus:outline-none">
                    <svg class="w-4 h-4 text-white dark:text-gray-800 rtl:rotate-180" aria-hidden="true"
                        xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M5 1 1 5l4 4" />
                    </svg>
                    <span class="sr-only">Previous</span>
                </span>
            </button>
            <button type="button"
                class="absolute top-0 end-0 z-30 flex items-center justify-center h-full px-4 cursor-pointer group focus:outline-none"
                data-carousel-next>
                <span
                    class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-white/30 dark:bg-gray-800/30 group-hover:bg-white/50 dark:group-hover:bg-gray-800/60 group-focus:ring-4 group-focus:ring-white dark:group-focus:ring-gray-800/70 group-focus:outline-none">
                    <svg class="w-4 h-4 text-white dark:text-gray-800 rtl:rotate-180" aria-hidden="true"
                        xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="m1 9 4-4-4-4" />
                    </svg>
                    <span class="sr-only">Next</span>
                </span>
            </button>
        </div>


        <!-- Features Section -->
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8 p-4">
            <div class="flex flex-col items-center bg-white rounded-lg p-4 shadow-sm">
                <div class="w-12 h-12 flex items-center justify-center bg-blue-100 rounded-full mb-2">
                    <i class="fas fa-truck text-blue-600"></i>
                </div>
                <h3 class="text-sm font-medium text-center">Free Shipping</h3>
            </div>
            <div class="flex flex-col items-center bg-white rounded-lg p-4 shadow-sm">
                <div class="w-12 h-12 flex items-center justify-center bg-blue-100 rounded-full mb-2">
                    <i class="fas fa-undo text-blue-600"></i>
                </div>
                <h3 class="text-sm font-medium text-center">Money Guarantee</h3>
            </div>
            <div class="flex flex-col items-center bg-white rounded-lg p-4 shadow-sm">
                <div class="w-12 h-12 flex items-center justify-center bg-blue-100 rounded-full mb-2">
                    <i class="fas fa-headset text-blue-600"></i>
                </div>
                <h3 class="text-sm font-medium text-center">24/7 Customer Support</h3>
            </div>
            <div class="flex flex-col items-center bg-white rounded-lg p-4 shadow-sm">
                <div class="w-12 h-12 flex items-center justify-center bg-blue-100 rounded-full mb-2">
                    <i class="fas fa-credit-card text-blue-600"></i>
                </div>
                <h3 class="text-sm font-medium text-center">Secure Payment</h3>
            </div>
        </div>

        <!-- New Arrivals Section -->
        <div class="mb-8">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-xl font-bold">New arrivals</h2>
                <a href="#" class="text-blue-600 text-sm font-medium">View all</a>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% newArrivals.forEach(product=>{ %>
                    <!-- Product Card 1 -->
   <div class="group bg-white rounded-2xl shadow-sm hover:shadow-xl transition-all overflow-hidden border relative">

    <!-- Discount Badge -->
    <% if (product.discount && product.discount > 0) { %>
        <div class="absolute top-3 left-3 z-10">
            <span class="bg-orange-600 text-white text-xs font-bold px-2 py-1 rounded-full shadow">
                <%= product.discount %>% OFF
            </span>
        </div>
    <% } %>

    <!-- Wishlist / Heart Icon -->
    <div class="absolute top-3 right-3 z-10">
        <% if (product.inCart) { %>
            <div class="bg-white/90 p-2 rounded-full shadow-md cursor-not-allowed">
                <i class="fa-solid fa-heart text-gray-300 opacity-60 text-xl"></i>
            </div>
        <% } else { %>
            <a href="/wishlist/toggle/<%= product._id %>"
               class="wishlist-btn bg-white p-2 rounded-full shadow-md hover:shadow-lg 
                      transition-all duration-300 active:scale-90 inline-flex">

                <i class="
                    fa-heart text-xl transition-colors duration-300
                    <%= product.inWishlist 
                        ? 'fa-solid text-red-500'
                        : 'fa-regular text-gray-500 hover:text-red-400'
                    %>
                "></i>
            </a>
        <% } %>
    </div>

    <!-- Product Image -->
    <div class="w-full h-44 bg-gray-50 flex items-center justify-center overflow-hidden">
        <img src="<%= product.productImage[0]?.url %>"
             onclick="window.location.href='/productdetails/<%= product._id %>'"
             class="w-full h-full object-contain p-3 group-hover:scale-105 transition cursor-pointer" />
    </div>

    <!-- Product Details -->
    <div class="p-4">

        <!-- Product Name -->
        <h3 class="font-semibold text-sm text-gray-800 h-10 overflow-hidden leading-tight">
            <%= product.productName %>
        </h3>

        <!-- Price Section -->
        <div class="flex gap-2 items-center mt-1">
            <!-- Discounted Price -->
            <p class="text-purple-700 font-bold text-lg">
                ₹<%= Math.round(product.price * (1 - (product.discount || 0) / 100)) %>
            </p>

            <% if (product.discount && product.discount > 0) { %>
                <!-- Original Price -->
                <p class="text-gray-500 text-sm mt-1 line-through">
                    ₹<%= product.price %>
                </p>
            <% } %>
        </div>

        <!-- Rating (Static for now) -->
        <div class="flex items-center gap-1 mt-1">
            <i class="fas fa-star text-yellow-400 text-xs"></i>
            <span class="text-gray-500 text-xs">(5)</span>
        </div>

        <!-- Add to Cart Button -->
        <button
            class="toggle-cart-btn mt-3 w-full text-center rounded-full py-2 font-medium text-white
                   transition
                   <%= product.inCart ? 'bg-green-600 hover:bg-green-700' : 'bg-purple-600 hover:bg-purple-700' %>"
            data-product-id="<%= product._id %>"
            data-in-cart="<%= product.inCart %>">

            <i class="fas fa-shopping-cart text-xs mr-1"></i>
            <span><%= product.inCart ? 'Go to Cart' : 'Add to Cart' %></span>
        </button>

    </div>
</div>

<% }) %>

                

            </div>
        </div>

        <!-- Trending Products Section -->
        <div class="mb-8">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-xl font-bold">Trending products</h2>
                <a href="#" class="text-blue-600 text-sm font-medium">View all</a>
            </div>

            <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
                <%trendingProducts.forEach(product=>{%>
                   <div class="group bg-white rounded-2xl shadow-sm hover:shadow-xl transition-all overflow-hidden border relative">

    <!-- Discount Badge -->
    <% if (product.discount && product.discount > 0) { %>
        <div class="absolute top-3 left-3 z-10">
            <span class="bg-orange-600 text-white text-xs font-bold px-2 py-1 rounded-full shadow">
                <%= product.discount %>% OFF
            </span>
        </div>
    <% } %>

    <!-- Wishlist / Heart Icon -->
    <div class="absolute top-3 right-3 z-10">
        <% if (product.inCart) { %>
            <div class="bg-white/90 p-2 rounded-full shadow-md cursor-not-allowed">
                <i class="fa-solid fa-heart text-gray-300 opacity-60 text-xl"></i>
            </div>
        <% } else { %>
            <a href="/wishlist/toggle/<%= product._id %>"
               class="wishlist-btn bg-white p-2 rounded-full shadow-md hover:shadow-lg 
                      transition-all duration-300 active:scale-90 inline-flex">

                <i class="
                    fa-heart text-xl transition-colors duration-300
                    <%= product.inWishlist 
                        ? 'fa-solid text-red-500'
                        : 'fa-regular text-gray-500 hover:text-red-400'
                    %>
                "></i>
            </a>
        <% } %>
    </div>

    <!-- Product Image -->
    <div class="w-full h-44 bg-gray-50 flex items-center justify-center overflow-hidden">
        <img src="<%= product.productImage[0]?.url %>"
             onclick="window.location.href='/productdetails/<%= product._id %>'"
             class="w-full h-full object-contain p-3 group-hover:scale-105 transition cursor-pointer" />
    </div>

    <!-- Product Details -->
    <div class="p-4">

        <!-- Product Name -->
        <h3 class="font-semibold text-sm text-gray-800 h-10 overflow-hidden leading-tight">
            <%= product.productName %>
        </h3>

        <!-- Price Section -->
        <div class="flex gap-2 items-center mt-1">
            <!-- Discounted Price -->
            <p class="text-purple-700 font-bold text-lg">
                ₹<%= Math.round(product.price * (1 - (product.discount || 0) / 100)) %>
            </p>

            <% if (product.discount && product.discount > 0) { %>
                <!-- Original Price -->
                <p class="text-gray-500 text-sm mt-1 line-through">
                    ₹<%= product.price %>
                </p>
            <% } %>
        </div>

        <!-- Rating (Static for now) -->
        <div class="flex items-center gap-1 mt-1">
            <i class="fas fa-star text-yellow-400 text-xs"></i>
            <span class="text-gray-500 text-xs">(5)</span>
        </div>

        <!-- Add to Cart Button -->
        <button
            class="toggle-cart-btn mt-3 w-full text-center rounded-full py-2 font-medium text-white
                   transition
                   <%= product.inCart ? 'bg-green-600 hover:bg-green-700' : 'bg-purple-600 hover:bg-purple-700' %>"
            data-product-id="<%= product._id %>"
            data-in-cart="<%= product.inCart %>">

            <i class="fas fa-shopping-cart text-xs mr-1"></i>
            <span><%= product.inCart ? 'Go to Cart' : 'Add to Cart' %></span>
        </button>

    </div>
</div>

<% }) %>

            </div>
        </div>

        <!-- Weekly Sale Banner -->
        <div class="bg-blue-100 rounded-lg p-6 mb-8 flex items-center justify-between">
            <div>
                <h3 class="text-2xl font-bold text-blue-900 mb-2">SEASONAL WEEKLY SALE 2024</h3>
                <p class="text-blue-800 mb-4">Get up to 20% off on selected items</p>
                <button class="bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-6 rounded-md">Shop
                    Now</button>
            </div>

            <div class="hidden md:block">
                <img src="https://images.unsplash.com/photo-1555041469-a586c61ea9bc?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=300&q=80"
                    alt="Sale" class="h-24 w-32 object-cover rounded-md">
            </div>
        </div>

        <!-- Special Offers Section -->
        <div class="mb-8">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-xl font-bold">Special offers for you</h2>
                <div class="flex space-x-2">
                    <button class="w-6 h-6 rounded-full bg-red-500"></button>
                    <button class="w-6 h-6 rounded-full bg-blue-500"></button>
                    <button class="w-6 h-6 rounded-full bg-green-500"></button>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% specialOffers.forEach(product=>{%>
                  <div class="group bg-white rounded-2xl shadow-sm hover:shadow-xl transition-all overflow-hidden border relative">

    <!-- Discount Badge -->
    <% if (product.discount && product.discount > 0) { %>
        <div class="absolute top-3 left-3 z-10">
            <span class="bg-orange-600 text-white text-xs font-bold px-2 py-1 rounded-full shadow">
                <%= product.discount %>% OFF
            </span>
        </div>
    <% } %>

    <!-- Wishlist / Heart Icon -->
    <div class="absolute top-3 right-3 z-10">
        <% if (product.inCart) { %>
            <div class="bg-white/90 p-2 rounded-full shadow-md cursor-not-allowed">
                <i class="fa-solid fa-heart text-gray-300 opacity-60 text-xl"></i>
            </div>
        <% } else { %>
            <a href="/wishlist/toggle/<%= product._id %>"
               class="wishlist-btn bg-white p-2 rounded-full shadow-md hover:shadow-lg 
                      transition-all duration-300 active:scale-90 inline-flex">

                <i class="
                    fa-heart text-xl transition-colors duration-300
                    <%= product.inWishlist 
                        ? 'fa-solid text-red-500'
                        : 'fa-regular text-gray-500 hover:text-red-400'
                    %>
                "></i>
            </a>
        <% } %>
    </div>

    <!-- Product Image -->
    <div class="w-full h-44 bg-gray-50 flex items-center justify-center overflow-hidden">
        <img src="<%= product.productImage[0]?.url %>"
             onclick="window.location.href='/productdetails/<%= product._id %>'"
             class="w-full h-full object-contain p-3 group-hover:scale-105 transition cursor-pointer" />
    </div>

    <!-- Product Details -->
    <div class="p-4">

        <!-- Product Name -->
        <h3 class="font-semibold text-sm text-gray-800 h-10 overflow-hidden leading-tight">
            <%= product.productName %>
        </h3>

        <!-- Price Section -->
        <div class="flex gap-2 items-center mt-1">
            <!-- Discounted Price -->
            <p class="text-purple-700 font-bold text-lg">
                ₹<%= Math.round(product.price * (1 - (product.discount || 0) / 100)) %>
            </p>

            <% if (product.discount && product.discount > 0) { %>
                <!-- Original Price -->
                <p class="text-gray-500 text-sm mt-1 line-through">
                    ₹<%= product.price %>
                </p>
            <% } %>
        </div>

        <!-- Rating (Static for now) -->
        <div class="flex items-center gap-1 mt-1">
            <i class="fas fa-star text-yellow-400 text-xs"></i>
            <span class="text-gray-500 text-xs">(5)</span>
        </div>

        <!-- Add to Cart Button -->
        <button
            class="toggle-cart-btn mt-3 w-full text-center rounded-full py-2 font-medium text-white
                   transition
                   <%= product.inCart ? 'bg-green-600 hover:bg-green-700' : 'bg-purple-600 hover:bg-purple-700' %>"
            data-product-id="<%= product._id %>"
            data-in-cart="<%= product.inCart %>">

            <i class="fas fa-shopping-cart text-xs mr-1"></i>
            <span><%= product.inCart ? 'Go to Cart' : 'Add to Cart' %></span>
        </button>

    </div>
</div>
<% }) %>
            </div>

            <!-- Special Offer 2 -->


    </main>
    <%-include("../../views/partials/user/footer")-%>
    <script>
          const isLoggedIn = ' <%= isLoggedIn ? "true" : "false" %>';

        document.addEventListener("DOMContentLoaded", () => {
            document.querySelectorAll(".toggle-cart-btn").forEach(button => {
                button.addEventListener("click", async () => {
                    if (isLoggedIn === "false") {
                        window.location.href = "/login";
                        return;
                    }

                    const productId = button.dataset.productId;
                    const isInCart = button.dataset.inCart === "true";

                    if (isInCart) {
                        window.location.href = "/cart";
                        return;
                    }

                    try {
                        const response = await fetch(`/add-to-cart/${productId}`, { method: "GET" });
                        if (response.ok) {
                            button.dataset.inCart = "true";
                            button.classList.remove("bg-purple-600");
                            button.classList.add("bg-green-600");
                            button.querySelector("span").innerText = "Go to Cart";
                        } else {
                            alert("Failed to add item to cart.");
                        }
                    } catch (error) {
                        console.error(error);
                        alert("Something went wrong.");
                    }
                });
            });
        });

    </script>