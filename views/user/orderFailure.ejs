<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Failed - MambraVille</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" />
</head>

<body class="bg-gray-50">

  <header class="bg-white shadow-sm">
    <%- include("../../views/partials/user/header") %>
  </header>

  <div class="container mx-auto py-8 sm:py-12 md:py-16 lg:py-20 px-4 sm:px-6 lg:px-8 max-w-3xl">
    <div class="bg-white p-6 sm:p-8 md:p-10 lg:p-12 rounded-xl shadow-lg">

      <!-- Error Icon -->
      <div class="flex justify-center mb-6">
        <div class="w-16 h-16 sm:w-20 sm:h-20 md:w-24 md:h-24 bg-red-100 rounded-full flex items-center justify-center">
          <i class="fas fa-times-circle text-red-600 text-4xl sm:text-5xl md:text-6xl"></i>
        </div>
      </div>

      <!-- Title -->
      <h1 class="text-2xl sm:text-3xl md:text-4xl font-bold mb-3 sm:mb-4 text-red-600 text-center">
        Oops! ❌
      </h1>

      <!-- Message -->
      <p class="text-base sm:text-lg text-gray-700 mb-3 sm:mb-4 text-center">
        Your order could not be placed successfully.
      </p>

      <!-- Error Details -->
      <div class="bg-red-50 border border-red-200 rounded-lg p-4 sm:p-5 mb-6 sm:mb-8">
        <p class="text-sm sm:text-base text-gray-600 text-center">
          Something went wrong during the payment or order processing.
        </p>
        <p class="text-sm sm:text-base text-gray-600 text-center mt-2">
          Please try again or contact support if the issue persists.
        </p>
      </div>

      <!-- Action Buttons -->
      <div class="flex flex-col sm:flex-row justify-center gap-3 sm:gap-4">
        <a href="/order/details/<%= order._id %>"
          class="w-full sm:w-auto px-6 sm:px-8 py-3 sm:py-3.5 bg-red-600 hover:bg-red-700 text-white rounded-lg font-medium text-center transition-all duration-200 shadow-sm hover:shadow-md text-sm sm:text-base">
          <i class="fas fa-receipt mr-2"></i>
          <span>View Order Details</span>
        </a>
        <a href="/products-user"
          class="w-full sm:w-auto px-6 sm:px-8 py-3 sm:py-3.5 border-2 border-gray-300 hover:border-gray-400 text-gray-700 hover:bg-gray-50 rounded-lg font-medium text-center transition-all duration-200 text-sm sm:text-base">
          <i class="fas fa-store mr-2"></i>
          <span>Continue Shopping</span>
        </a>
      </div>

      <!-- Help Section -->
      <div class="mt-8 sm:mt-10 pt-6 sm:pt-8 border-t border-gray-200">
        <p class="text-xs sm:text-sm text-gray-500 text-center mb-3">
          Need help? Contact our support team
        </p>
        <div class="flex flex-col sm:flex-row justify-center gap-2 sm:gap-4 text-xs sm:text-sm">
          <a href="mailto:support@mambraville.com" class="text-indigo-600 hover:text-indigo-700 text-center">
            <i class="fas fa-envelope mr-1"></i>
            <span>support@mambraville.com</span>
          </a>
          <span class="hidden sm:inline text-gray-400">|</span>
          <a href="tel:+911234567890" class="text-indigo-600 hover:text-indigo-700 text-center">
            <i class="fas fa-phone mr-1"></i>
            <span>+91 123 456 7890</span>
          </a>
        </div>
      </div>

    </div>
  </div>

  <%- include("../../views/partials/user/footer") %>

</body>

</html>