<div id="sidebar" class="w-64 bg-gradient-to-b from-black to-purple-900 text-white fixed inset-y-0 left-0 transform -translate-x-full md:relative md:translate-x-0 transition-transform duration-300 ease-in-out z-20">
    <div class="p-4 flex items-center justify-center">
        <img src="/logo.png" alt="Mambraville" class="h-12">
    </div>
    <nav class="mt-6">
        <a href="#" class="nav-link flex items-center px-6 py-3 hover:bg-purple-800">
            <i class="fas fa-home mr-3"></i><span>Dashboard</span>
        </a>
        <a href="/admin/products" class="nav-link flex items-center px-6 py-3 hover:bg-purple-800">
            <i class="fas fa-box mr-3"></i><span>Products</span>
        </a>
        <a href="/admin/users" class="nav-link flex items-center px-6 py-3 hover:bg-purple-800">
            <i class="fas fa-users mr-3"></i><span>Users</span>
        </a>
        <a href="/admin/orders" class="nav-link flex items-center px-6 py-3 hover:bg-purple-800">
            <i class="fas fa-shopping-cart mr-3"></i><span>Orders</span>
        </a>
        <a href="/admin/category" class="nav-link flex items-center px-6 py-3 hover:bg-purple-800">
            <i class="fas fa-tags mr-3"></i><span>Category</span>
        </a>
        <a href="#" class="nav-link flex items-center px-6 py-3 hover:bg-purple-800">
            <i class="fas fa-ticket-alt mr-3"></i><span>Coupons</span>
        </a>
        <a href="#" class="nav-link flex items-center px-6 py-3 hover:bg-purple-800">
            <i class="fas fa-gift mr-3"></i><span>Offers</span>
        </a>
        <a href="/admin/brands" class="nav-link flex items-center px-6 py-3 hover:bg-purple-800">
            <i class="fas fa-copyright mr-3"></i><span>Brands</span>
        </a>
    </nav>
    <div class="absolute bottom-4 left-0 w-full px-6">
        <button onclick="logout()" class="bg-white text-black py-2 mt-2  px-4 rounded-full w-full font-medium">
            Logout
        </button>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const links = document.querySelectorAll(".nav-link");
        const currentPath = window.location.pathname;

        links.forEach(link => {
            if (link.getAttribute("href") === currentPath) {
                link.classList.add("bg-purple-900", "text-white"); // Add active styles
            }
        });
    });
</script> 